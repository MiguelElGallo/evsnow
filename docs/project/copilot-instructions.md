# Copilot Instructions

The canonical Copilot instructions remain in the operational GitHub file:

- Source file: [`.github/copilot-instructions.md`](https://github.com/MiguelElGallo/evsnow/blob/main/.github/copilot-instructions.md)

That file is intentionally kept outside the Zensical `docs_dir` so GitHub
Copilot can read it directly. Update the `.github` file first when changing
agent guidance, then update this pointer if the file moves.
